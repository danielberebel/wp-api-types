export interface TagWpInterface {
    id?: number; // Read only
    count?: number; // Read only
    description?: string;
    link?: string;
    uri?: string; // Read only
    name?: string;
    slug?: string;
    taxonomy?: string; // Read only
    // meta_data
}
